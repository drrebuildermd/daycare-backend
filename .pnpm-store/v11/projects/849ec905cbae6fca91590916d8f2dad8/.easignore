# EAS 빌드 서버로 업로드할 때 제외할 것들.
# 이 프로젝트는 git 저장소가 아니라 .gitignore가 없으므로, 이 파일이 없으면
# node_modules 전체가 업로드되어 빌드가 매우 느려진다.

node_modules/
.expo/
dist/
web-build/

# .env 는 로컬 웹 개발용(127.0.0.1)이다. 폰에 설치할 APK 는 이 주소로 붙으면 안 되므로
# 업로드에서 제외하고, 빌드용 값은 eas.json 의 env 만 쓰도록 한다.
.env

*.log
*.bak
